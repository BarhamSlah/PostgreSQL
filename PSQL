* GUI Tools (Graphical User Interface): Programs like pgAdmin or DBeaver provide a visual interface with buttons and 
to interact with the PostgreSQL database.

Command Line Interface (CLI): Tools like psql allow you to directly type SQL commands and Postgres commands in the Terminal
or Command Prompt.

Why is there an emphasis on the Terminal?

Learning the Terminal initially gives you a strong understanding of fundamental SQL commands without relying on a graphical interface.

In real-world work environments (such as remote servers), you rarely have access to a GUI, making CLI knowledge essential.

* Setting up psql and Environment Variables (Minutes 18:30 to 21:40)

Accessing psql: On Windows, you can access the SQL Shell directly through the psql application or via Command Prompt / PowerShell.

*****************************************************************************************************************************************
How to Store Data : 
1 - Strores data in table
2 - Table consist of (Row & colomn)

****************************************************************************************************************************************
That is normal if 2 or more table have relationship between each other

The best way for learning any database is by using the actual shell command line

*****************************************************************************************************************************************
* Use uppercase syntax for SQL command 

CREATE DATABASE nameofdatabase;   // to create database
\l   // to display all databases

**************************************************************************************************************************************
1 - (psql --help) is a terminal command line flag. It can only be used in your operating system's command line 
(PowerShell, Command Prompt, or Terminal) before opening PostgreSQL.


2 - Inside the psql Shell (psql --help) this not work so instead of this line use one of those : 
Use internal backslash meta-commands instead:

1 - \? — Shows help for psql slash commands   &&   2 - \h — Shows help for SQL commands

**************************************************************************************************************************************
HOW TO CONNECT DATABASE 

1 - In psql (\c nameofDB) 

2 - Not psql (psql -h localhost -p 5432 -U postgres test)

**************************************************************************************************************************************
\dt   // List all tables in the test database:

\conninfo   // Check connection details:

**************************************************************************************************************************************
HOW TO DELETE DATABASE :

DROP DATABASE NAMEOFDATABASE;   // by this line the database will gone

when you can do that ? 1- if you are not already in the database on psql or powershell ...etc  &&  2- disconnect with IDE (stop button)

when you can back this database ? if you backup it manually before 

**************************************************************************************************************************************
HOW TO CREATE TABLE WITHOUT CONSTRAINS : 

CREATE TABLE person (
    id INT,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    birth_year INT
);

**************************************************************************************************************************************
 

